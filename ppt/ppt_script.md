외형 분류 1장
외형 분류의 경우, 1차 때 방식은 데이터가 많은 9개 클래스만 분류하고 나머지는 기타 처리하는 방식
이번 2차에서는 29개를 모두 분류합니다.

검색 과정
    이미지 입력 => 
    알약 영역 분리 (이미지 설명) => 
    알약 모양을 숫자로 이루어진 벡터로 변환 => 
    각 외형의 대표 벡터와 거리 비교 => 
    가장 가까운 클래스 후보들 반환


외형 분류 2장
56차원 형상 임베딩 다음으로 구성
각 차원은 해당 숫자 개수로 이루어진 벡터
    기본 기하 특징 : 얼마나 길쭉하고, 둥글고, 오목한가를 벡터로 표현
    EFD : 외곽선 좌표 벡터로 전환
    방사형 FFT : 중심에서 외곽까지 거리가 방향에 따라 변하는 것을 벡터로 표현
    Hu moment : 면적 분포의 패턴을 벡터화

벡터 생성 과정
5만4천장의 이미지 특징을 2만7천개의 알약별 중앙값으로 모으고
다시 외형별로 29개의 중앙값을 모아서 대표 벡터로 만들었습니다.

외형 분류 3장
실제 이미지에서 확인한 외형 검색 결과는
보시는바와 같이 장방형, 사과형, 땅콩형
